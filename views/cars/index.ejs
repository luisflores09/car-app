<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs'); %>
    <title>Car-App Cars Index Page</title>
</head>

<body>
    <header>
        <%- include('../partials/header.ejs'); %>
    </header>
    <main>
        <h1>List of Cars</h1>
        <nav>
            <a href="/cars/new">Create New Car</a>
        </nav>
        <ul>
            <% for(let i = 0; i < cars.length; i++){ %>
            <li>
                <a href="/cars/<%=cars[i]._id%>"><%= cars[i].name %></a><br>
                <button>
                    <a href="/cars/<%=cars[i]._id%>/edit">Edit</a>
                </button>
                <form action="/cars/<%= cars[i]._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE">
                </form>
            </li>
            <img src="<%= cars[i].image %>">
            <br><br>
            <% } %>
        </ul>
    </main>
    <footer>
        <%- include('../partials/footer.ejs'); %>
    </footer>
</body>

</html>